{
  "name": "skilltree-server",
  "version": "1.0.0",
  "description": "Local server for the SkillTree app prototype — serves the static app and proxies AI mentor requests to the Anthropic API.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
